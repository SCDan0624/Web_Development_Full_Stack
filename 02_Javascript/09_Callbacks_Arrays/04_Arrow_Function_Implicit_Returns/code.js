// regular arrow function

const isEven = (num) => {
  return num % 2 === 0;
};

// implicit return (return keyword can be left off, only works with one statement)
const isEven = (num) => num % 2 === 0;
