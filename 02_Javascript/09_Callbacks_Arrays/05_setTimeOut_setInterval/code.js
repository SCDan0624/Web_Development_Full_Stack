// setTimeOut(function(){},millisecs)
setTimeout(() => {
  console.log("Hello!!");
}, 3000);

// setInterval
// runs function every certain amount of time
//  until clearInterval() is called
setInterval(() => {
  console.log(Math.random());
}, 3000);
