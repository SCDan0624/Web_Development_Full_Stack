/*
Variable visibility
The location where a variable is defined
 dictates where we have access to that variable
*/
function collectEggs() {
  let totalEggs = 6; // variable is only able to be accessed inside function
  console.log(totalEggs);
}
