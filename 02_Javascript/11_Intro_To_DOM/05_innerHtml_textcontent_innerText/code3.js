/* 
innerText

The innerText property sets or returns the text content 
of the specified node, and all its descendants.

Return the text content of node

node.innerText

Set the text content of node
node.innerText = text
 */

const x = document.getElementById("myBtn").innerText;
