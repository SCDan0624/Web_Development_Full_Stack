// String Methods are built in actions
// We can perform on indiviual strings

// Examples

// Casing
const cat = "Mo";
const dog = "Tiny";

dog.toUpperCase(); // 'TINY
cat.toLowerCase(); // "mo"

// original variables remained unchanged

// Trim
let greeting = "  leave me alone  ";
greeting.trim(); // 'leave me alone'
